# Knowledge-authoring tasks — open, and Christian's to close

Opened 20 September 2026. These are the questions the design work has found that **authoring** must
answer, not derivation. They accumulate here because they keep arriving one at a time in the middle of
other decisions, and because each one is a place where the engine will correctly refuse rather than
invent an answer.

**None of them blocks derivation-engine design.** Each blocks a specific game from resolving.

**His standing instruction on all of them:** the gaps stay explicit. *"Do not fill them from legacy
code."*

| # | What is missing | Where it bites | Ruled |
|---|---|---|---|
| 1 | **Where a carrier sits relative to the progression line.** RPC-001 does not own or instantiate its scoring carrier (KR-04), and the four retired code sentences were doing that job on its behalf. Nothing now authors the placement | Any RPC-001 activity: the carrier's position has no supported value | KR-05, 20 Sep |
| 2 | **Where a build-out restart is taken from.** RPC-PROP-001 authors the begin condition — "Goalkeeper controls possession or play restarts from a controlled defensive restart" — and says nothing about placement. The retired sentence said "in your own half", which no workbook authors | Any build-out episode | KR-05, 20 Sep |
| 3 | **Wide Zone Advantage's contract needs restating to use a lateral selector at whole-game scope.** Every one of its region items is scoped to its own involvement, which AM-13 empties, and no item anywhere carries the lateral selector AM-17 provides. Derivation will not do this on the contract's behalf — his ruling: "Do not repair Wide Zone through derivation" | Wide Zone's wide channels resolve as a declared gap (SD-31), so the game does not resolve | 20 Sep, recorded as a future task with **no action now** |
| 4 | **No value modifier anywhere declares an operation.** SD-30 makes a magnitude without an operation *incomplete*, so every modifier in the corpus is incomplete until its knowledge is amended | Every effective-value comparison; every activity carrying a scoring multiplier | SD-30, 20 Sep |
| 5 | **Eleven Game Forms leave a start or restart unauthored.** Measured 18 September. The model invents them today | Every activity using those forms | Open since 18 Sep |

## What is *not* on this list

- Anything derivation can decide. A gap here means no authored knowledge answers the question — not
  that the rules are unclear about who should.
- Anything the engine can refuse. A refusal is a working outcome; these are missing knowledge.
- The two comparative-grammar items still open (no aggregate function named; comparative cardinality
  unsupported). Those are grammar questions, and SD-34 says cardinality returns only with a concrete
  authored case that needs it.
